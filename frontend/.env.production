# Cloudflare Workers CRUD API (D1-backed, global edge)
VITE_API_URL=https://brivva-api.milliytechnology.workers.dev

# Media engine (server-rs): set to ECS GPU primary before May 10.
# Laptop fallback can use a Cloudflare Tunnel hostname here during emergency.
VITE_MEDIA_URL=https://brivva.spiko.uz
