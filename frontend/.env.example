# Cloudflare Workers CRUD API (D1-backed control plane)
VITE_API_URL=https://brivva-api.milliytechnology.workers.dev

# Media engine (server-rs): laptop GPU, ECS GPU, or Fargate CPU fallback.
# Frontend rewrites http→ws / https→wss for /api/session.
VITE_MEDIA_URL=https://brivva.spiko.uz
